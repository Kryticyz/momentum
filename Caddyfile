# Development: HTTP only on port 80.
:80 {
	reverse_proxy api:8080
}

# Production: uncomment the block below and comment out the :80 block above.
# Replace your-domain.com with your actual domain — Caddy handles HTTPS
# automatically via Let's Encrypt.
#
# your-domain.com {
# 	reverse_proxy api:8080
# }
